# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'git_commit_pre_hook' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for git_commit_pre_hook

  target 'git_commit_pre_hookTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'git_commit_pre_hookUITests' do
    # Pods for testing
  end

end
